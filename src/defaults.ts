export enum DEFAULTS {
  SEED = 0,
  // Noise
  PERSISTENCE = 0.5,
  AMPLITUDE = 1,
  OCTAVES = 5,
  WAVELENGTH = 133,
  // Terrain
  EXPONENT = 3.3,
  PEAKS = 0.25,
  WATER_LEVEL = 132,
  BEACH_SIZE = 12,
}
